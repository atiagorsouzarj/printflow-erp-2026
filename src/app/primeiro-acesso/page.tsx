import {setupAdmin} from "@/app/auth-actions";import {db} from "@/db";import {users} from "@/db/schema";import {sql} from "drizzle-orm";import {redirect} from "next/navigation";import {KeyRound,ShieldCheck} from "lucide-react";export default async function Page({searchParams}:{searchParams:Promise<{erro?:string}>}){const q=await searchParams;const [{count}]=await db.select({count:sql<number>`count(*)::int`}).from(users);if(count>0)redirect("/login");return <main className="auth-page"><section className="auth-card setup-card"><div className="public-brand"><div className="brand-mark"><i/><i/><i/></div><strong>PrintFlow ERP</strong></div><div className="setup-icon"><KeyRound/></div><p className="eyebrow">Configuração única</p><h1>Primeiro administrador</h1><p>Crie a conta responsável pela implantação. Esta tela será desativada automaticamente após o cadastro.</p>{q.erro&&<div className="auth-error">Não foi possível criar o administrador. Revise os dados, a senha e o token.</div>}<form action={setupAdmin}><label>Nome completo<input name="name" required minLength={2} autoComplete="name"/></label><label>E-mail empresarial<input name="email" type="email" required autoComplete="email"/></label><label>Senha forte<input name="password" type="password" minLength={12} required autoComplete="new-password"/></label><label>Token de implantação<input name="setupToken" type="password" autoComplete="off"/></label><div className="password-hint"><ShieldCheck/> Mínimo de 12 caracteres. Em produção, use o ADMIN_SETUP_TOKEN.</div><button className="button button-primary">Criar administrador e entrar</button></form></section></main>}
