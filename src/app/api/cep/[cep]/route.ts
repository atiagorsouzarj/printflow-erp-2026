export async function GET(_:Request,{params}:{params:Promise<{cep:string}>}){const {cep}=await params;const clean=cep.replace(/\D/g,"");if(clean.length!==8)return Response.json({error:"CEP inválido"},{status:400});const response=await fetch(`https://viacep.com.br/ws/${clean}/json/`,{next:{revalidate:86400}});if(!response.ok)return Response.json({error:"Falha na consulta"},{status:502});const data=await response.json();if(data.erro)return Response.json({error:"CEP não encontrado"},{status:404});return Response.json({cep:data.cep,address:data.logradouro,neighborhood:data.bairro,city:data.localidade,uf:data.uf});}
