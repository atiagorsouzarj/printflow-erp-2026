import {addPrinterCategory} from "@/app/actions";import {PrinterEditor} from "@/components/printer-editor";import {PrinterTabs} from "@/components/printer-tabs";import {Card,Drawer,Field,PageHeader,Submit,money} from "@/components/ui";import {db} from "@/db";import {printerCategories,printerConsumables,printers} from "@/db/schema";import {Droplets,Gauge,Printer} from "lucide-react";export const dynamic="force-dynamic";export default async function Page(){const [categories,printerRows,consumables]=await Promise.all([db.select().from(printerCategories).orderBy(printerCategories.name),db.select().from(printers).orderBy(printers.name),db.select().from(printerConsumables)]);const investment=printerRows.reduce((s,p)=>s+Number(p.acquisitionCost),0),avg=printerRows.length?printerRows.reduce((s,p)=>s+Number(p.colorCost),0)/printerRows.length:0;return <><PageHeader eyebrow="Categoria profissional" title="Impressoras & Precificação" description="Navegue por tecnologia; cada aba possui fórmula, campos e equipamentos próprios." action={<div className="header-actions"><Drawer label="Categorias" title="Nova categoria"><form action={addPrinterCategory} className="form"><div className="form-grid"><Field label="Nome"><input name="name" required/></Field><Field label="Tecnologia"><select name="technology"><option value="laser">Laser CMYK</option><option value="inkjet">Jato de tinta</option><option value="cutter">Plotter de recorte</option><option value="thermal">Térmica</option><option value="sublimation">Sublimação</option><option value="three_d">Impressão 3D</option><option value="custom">Personalizada</option></select></Field><Field label="Método"><select name="pricingMode"><option value="click">Página / clique</option><option value="time">Hora máquina</option><option value="area">Área</option><option value="weight">Peso</option></select></Field><Field label="Descrição"><textarea name="description"/></Field></div><div className="form-actions"><Submit>Salvar</Submit></div></form></Drawer><PrinterEditor categories={categories}/></div>}/><div className="metrics"><Card className="metric"><div className="metric-icon tone-blue"><Printer/></div><div><p>Equipamentos</p><strong>{printerRows.length}</strong><small>cadastrados</small></div></Card><Card className="metric"><div className="metric-icon tone-purple"><Gauge/></div><div><p>Categorias</p><strong>{categories.length}</strong><small>abas tecnológicas</small></div></Card><Card className="metric"><div className="metric-icon tone-amber"><Droplets/></div><div><p>Imobilizado</p><strong>{money(investment)}</strong><small>aquisição total</small></div></Card><Card className="metric"><div className="metric-icon tone-green"><Gauge/></div><div><p>Custo médio cor</p><strong>{money(avg)}</strong><small>referência a 5%</small></div></Card></div><PrinterTabs categories={categories} printers={printerRows} consumables={consumables}/><Card className="pricing-note"><strong>Regra central</strong><span>Equipamento calcula impressão/corte/hora.</span><span>Material calcula papel, etiqueta, camisa ou filamento.</span><span>Produto combina todos os processos e aplica margem.</span></Card></>}
