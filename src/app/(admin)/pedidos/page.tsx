import {updateOrderDetails} from "@/app/actions";import {Badge,Card,CardTitle,Drawer,Field,PageHeader,Submit,money,shortDate} from "@/components/ui";import {db} from "@/db";import {clients,orders} from "@/db/schema";import {desc,eq} from "drizzle-orm";import {ExternalLink,Printer,Settings2} from "lucide-react";import Link from "next/link";export const dynamic="force-dynamic";export default async function Page(){const rows=await db.select({order:orders,client:clients}).from(orders).innerJoin(clients,eq(orders.clientId,clients.id)).orderBy(desc(orders.createdAt));return <><PageHeader eyebrow="Do fechamento à entrega" title="Pedidos" description="Numeração oficial, prazo, produção, pagamento, logística e acompanhamento do cliente."/><Card><CardTitle detail="A numeração automática é configurada no Painel de Controle e pode ser ajustada aqui">Carteira de pedidos</CardTitle><div className="table-wrap"><table className="table"><thead><tr><th>Pedido</th><th>Cliente</th><th>Previsão</th><th>Produção</th><th>Entrega</th><th>Pagamento</th><th>Total</th><th>Ações</th></tr></thead><tbody>{rows.map(({order:o,client:c})=><tr key={o.id}><td><strong>{o.code}</strong><br/><small>{shortDate(o.createdAt)}</small></td><td>{c.name}<br/><small>{c.whatsapp||c.mobile||c.phone}</small></td><td>{shortDate(o.dueDate)}</td><td><Badge tone={o.stage==="concluido"?"green":"blue"}>{o.stage}</Badge></td><td><Badge tone={o.deliveryStatus==="entregue"?"green":o.deliveryStatus==="em_transito"?"blue":"amber"}>{o.deliveryStatus.replaceAll("_"," ")}</Badge><br/><small>{o.deliveryMethod.replaceAll("_"," ")}</small></td><td><Badge tone={o.paymentStatus==="pago"?"green":"amber"}>{o.paymentStatus}</Badge></td><td><strong>{money(o.total)}</strong></td><td><div className="row-actions"><Link title="Imprimir" href={`/imprimir/${o.id}`} target="_blank"><Printer/></Link><Link title="PDF" href={`/api/pdf/pedido/${o.id}`} target="_blank"><ExternalLink/></Link>{o.publicToken&&<Link title="Acompanhamento público" href={`/acompanhar/${o.publicToken}`} target="_blank"><ExternalLink/></Link>}<Drawer label="Editar" title={`Editar ${o.code}`}><form action={updateOrderDetails} className="form"><input type="hidden" name="orderId" value={o.id}/><div className="form-grid"><Field label="Número do pedido"><input name="code" defaultValue={o.code} required/></Field><Field label="Previsão de entrega"><input name="dueDate" type="date" defaultValue={o.dueDate||""}/></Field><Field label="Modalidade"><select name="deliveryMethod" defaultValue={o.deliveryMethod}><option value="balcao">Balcão</option><option value="99entrega">99Entrega</option><option value="uber_entrega">Uber Entrega</option><option value="superfrete">SuperFrete</option><option value="a_definir">A definir</option></select></Field><Field label="Código de rastreio"><input name="trackingCode" defaultValue={o.trackingCode||""}/></Field></div><div className="form-actions"><Submit>Salvar pedido</Submit></div></form></Drawer></div></td></tr>)}</tbody></table></div></Card></>}
