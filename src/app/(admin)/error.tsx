"use client";import {AlertTriangle,RefreshCw} from "lucide-react";export default function ErrorPage({error,reset}:{error:Error&{digest?:string};reset:()=>void}){return <div className="system-error"><div className="metric-icon tone-amber"><AlertTriangle/></div><p className="eyebrow">Não foi possível concluir</p><h1>Algo interrompeu esta página.</h1><p>{error.message||"Tente novamente. Se o erro persistir, consulte os logs de integração."}</p>{error.digest&&<small>Referência: {error.digest}</small>}<button className="button button-primary" onClick={reset}><RefreshCw/> Tentar novamente</button></div>}
