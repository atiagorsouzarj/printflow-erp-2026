import { addLead } from "@/app/actions";
import { ClientForm } from "@/components/client-form";
import { Badge, Card, CardTitle, Drawer, Field, PageHeader, Submit, shortDate } from "@/components/ui";
import { db } from "@/db";import { clients, leads } from "@/db/schema";import { desc } from "drizzle-orm";import { Building2, ContactRound, MessagesSquare, UserPlus } from "lucide-react";
export const dynamic="force-dynamic";
export default async function CrmPage(){const [clientRows,leadRows]=await Promise.all([db.select().from(clients).orderBy(desc(clients.createdAt)),db.select().from(leads).orderBy(desc(leads.createdAt))]);return <><PageHeader eyebrow="Relacionamento" title="Clientes" description="Cadastros completos PF e PJ integrados a orçamentos, pedidos, PDV e comunicações." action={<div style={{display:"flex",gap:8}}><Drawer label="Lead manual" title="Cadastrar oportunidade"><form action={addLead} className="form"><input type="hidden" name="origin" value="manual"/><div className="form-grid"><Field label="Nome"><input name="name" required/></Field><Field label="Telefone / WhatsApp"><input name="phone"/></Field><Field label="E-mail"><input name="email" type="email"/></Field><Field label="Interesse"><input name="interest"/></Field></div><div className="form-actions"><Submit>Salvar na fila</Submit></div></form></Drawer><Drawer label="Novo cliente" title="Cadastro completo"><ClientForm/></Drawer></div>}/><div className="metrics"><Card className="metric"><div className="metric-icon tone-green"><ContactRound size={20}/></div><div><p>Clientes</p><strong>{clientRows.length}</strong><small>na base comercial</small></div></Card><Card className="metric"><div className="metric-icon tone-blue"><UserPlus size={20}/></div><div><p>Leads abertos</p><strong>{leadRows.filter(l=>l.status!=="convertido").length}</strong><small>na pré-triagem</small></div></Card><Card className="metric"><div className="metric-icon tone-amber"><MessagesSquare size={20}/></div><div><p>Com WhatsApp</p><strong>{clientRows.filter(c=>c.whatsapp||c.mobile||c.phone).length}</strong><small>contatos disponíveis</small></div></Card><Card className="metric"><div className="metric-icon tone-purple"><Building2 size={20}/></div><div><p>Pessoas jurídicas</p><strong>{clientRows.filter(c=>c.type==="pj").length}</strong><small>contas B2B</small></div></Card></div><Card><CardTitle detail="PF e PJ com identificação, endereço, canais e consentimentos">Carteira de clientes</CardTitle><div className="table-wrap"><table className="table"><thead><tr><th>Cliente</th><th>Tipo</th><th>Contato</th><th>Documento</th><th>Localização</th><th>Status</th><th>Desde</th></tr></thead><tbody>{clientRows.map(c=><tr key={c.id}><td><strong>{c.name}</strong><br/><small>{c.fantasyName||c.nickname||c.contactPerson}</small></td><td><Badge tone={c.type==="pj"?"purple":"blue"}>{c.type.toUpperCase()}</Badge></td><td>{c.whatsapp||c.mobile||c.phone||"—"}<br/><small>{c.email}</small></td><td>{c.document||"—"}</td><td>{[c.city,c.uf].filter(Boolean).join(" / ")||"—"}</td><td><Badge tone={c.status==="liberado"?"green":"red"}>{c.status}</Badge></td><td>{shortDate(c.createdAt)}</td></tr>)}</tbody></table></div></Card></>}
