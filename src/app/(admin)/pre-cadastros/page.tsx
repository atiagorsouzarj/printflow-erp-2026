import { addLead, convertLead } from "@/app/actions";import { Badge, Card, Drawer, Field, PageHeader, Submit, shortDate } from "@/components/ui";import { db } from "@/db";import { leads } from "@/db/schema";import { desc } from "drizzle-orm";import { Bot, PhoneCall, Store, UsersRound } from "lucide-react";
export const dynamic="force-dynamic";const tone=(origin:string)=>origin==="whatsapp"?"green":origin==="external_api"?"purple":origin==="balcao"?"amber":"blue" as const;
export default async function PrePage(){const rows=await db.select().from(leads).orderBy(desc(leads.createdAt));return <><PageHeader eyebrow="Captação multicanal" title="Pré-cadastros" description="Fila do bot, página pública, balcão e API externa antes da conversão para o CRM." action={<Drawer label="Novo manual" title="Nova captação"><form action={addLead} className="form"><div className="form-grid"><Field label="Nome / contato"><input name="name" required/></Field><Field label="Origem"><select name="origin"><option value="balcao">Balcão</option><option value="external_api">API externa</option><option value="manual">Outro</option></select></Field><Field label="Telefone"><input name="phone"/></Field><Field label="E-mail"><input name="email"/></Field><Field label="Interesse"><input name="interest"/></Field><Field label="Score"><input name="score" type="number" defaultValue="50"/></Field></div><div className="form-actions"><Submit>Adicionar à fila</Submit></div></form></Drawer>}/><div className="metrics"><Card className="metric"><div className="metric-icon tone-blue"><UsersRound size={20}/></div><div><p>Total captado</p><strong>{rows.length}</strong><small>todos os canais</small></div></Card><Card className="metric"><div className="metric-icon tone-green"><Bot size={20}/></div><div><p>Bot WhatsApp</p><strong>{rows.filter(x=>x.origin==="whatsapp").length}</strong><small>conversas captadas</small></div></Card><Card className="metric"><div className="metric-icon tone-amber"><Store size={20}/></div><div><p>Balcão / página</p><strong>{rows.filter(x=>["balcao","pagina_publica"].includes(x.origin)).length}</strong><small>entradas diretas</small></div></Card><Card className="metric"><div className="metric-icon tone-purple"><PhoneCall size={20}/></div><div><p>API externa</p><strong>{rows.filter(x=>x.origin==="external_api").length}</strong><small>integrações</small></div></Card></div><Card><div className="table-wrap"><table className="table"><thead><tr><th>Protocolo</th><th>Origem</th><th>Contato</th><th>Interesse</th><th>Score</th><th>Status</th><th>Ação</th></tr></thead><tbody>{rows.map(l=><tr key={l.id}><td><strong>{l.protocol||`PR-${l.id}`}</strong><br/><small>{shortDate(l.createdAt)}</small></td><td><Badge tone={tone(l.origin)}>{l.origin.replace("_"," ")}</Badge></td><td><strong>{l.name}</strong><br/><small>{l.phone} • {l.email}</small></td><td>{l.interest}<br/><small>{l.message}</small></td><td><div className="score"><i style={{width:`${l.score}%`}}/><strong>{l.score}</strong></div></td><td><Badge tone={l.status==="convertido"?"green":"gray"}>{l.status}</Badge></td><td>{l.status!=="convertido"&&<form action={convertLead}><input type="hidden" name="leadId" value={l.id}/><button className="button button-secondary button-small">→ CRM</button></form>}</td></tr>)}</tbody></table></div></Card></>}
